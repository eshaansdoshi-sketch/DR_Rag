# Production backend API URL — set this to your deployed Render backend URL
# This is used automatically during `next build` for production deployments
NEXT_PUBLIC_API_URL=https://dr-rag.onrender.com
